# تقرير فحص Voice Studio Standalone

**نطاق الفحص:** مراجعة المستودع كاملًا، الخادم `server.js`، الواجهة، المصادقة، التخزين، دورة حياة الصوت والوسائط، المهام الدورية، الاعتماديات، والاختبارات.

## الخلاصة التنفيذية

المشروع منظم نسبيًا ويحتوي على محاولات جيدة في المصادقة، Security Headers، التحقق من حالات الصوت، التزامن، التخزين الذري، وتنظيف الموارد. فحوصات الصياغة والاختبارات الحالية ناجحة: **16 اختبارًا ناجحًا دون فشل**.

لكن توجد نقطتان مهمتان. الأولى كانت خللًا منطقيًا في دورة حياة `Streamer`: عند انتهاء أو فشل مسار الكاميرا/البث كان الكود ينظف الوسيط مع `leaveVoice: true`، ما قد يرسل مغادرة للروم ويحوّل انقطاع الوسائط إلى خروج الحساب من الروم. تم إصلاح هذا في نسخة الفحص. الثانية أن الاعتماديات تحتوي على **4 ثغرات عالية** في سلسلة `ip → werift → node-av → @dank074/discord-video-stream`، وإصلاح npm المقترح يتطلب تغييرًا رئيسيًا ويجب اختباره قبل اعتماده.

## الإصلاحات المنفذة في نسخة الفحص

| المشكلة | التعديل |
| --- | --- |
| انتهاء مهمة الوسائط يخرج الحساب من الروم | أصبحت معالجة انتهاء/فشل مهمة الوسائط توقف مسار الوسائط فقط، ثم تطلب إعادة تشغيل ناقل جديد، دون `leaveVoice`. |
| استبدال ناقل وسائط بآخر قد يفصل اتصال الصوت | بدء تشغيل وسيط جديد لا يغادر الروم الأساسي، وكل تشغيل يحتفظ بمرجع مستقل إلى `Streamer` جديد. |
| فشل محاولة تشغيل الوسائط كان يستدعي مغادرة الحساب | أزيل استدعاء `leaveVoice` من مسار الفشل، وأصبح التنظيف يوقف مسار الوسائط فقط. |
| المصالحة الدورية قد تحذف جلسة مؤكدة أثناء نافذة إعادة التشغيل | أثناء وجود إعادة تشغيل وسائط معلقة، تحافظ المصالحة على جلسة الروم بدل حذفها فورًا. |
| تسرب حالة `accountOperations` عند كون الحساب أصلًا داخل الروم الهدف | أصبح مسار `alreadyIn` يغلق العملية صراحة قبل العودة. |

## نتائج التحقق

تم تشغيل الأوامر التالية بعد التعديل:

```text
npm run check   ✅
npm test        ✅ 16 passed, 0 failed
 git diff --check ✅
```

لم يتم إجراء اختبار اتصال Discord حقيقي أو اختبار WebRTC فعلي؛ ذلك يتطلب حسابًا مصرحًا به، اتصالًا حيًا، ورومًا صوتيًا مناسبًا. لذلك لا يمكن ضمان سلوك Discord الخارجي بنسبة مطلقة من اختبارات الوحدة فقط.

## ملاحظات أمنية

1. **ثغرات الاعتماديات:** `npm audit --omit=dev --audit-level=moderate` أظهر أربع ثغرات عالية مرتبطة بـ SSRF classification في حزمة `ip` عبر الاعتماديات الخاصة بالبث. أداة npm تقترح `npm audit fix --force`، لكنها تقترح إصدارًا رئيسيًا مختلفًا من `@dank074/discord-video-stream`. لا أنصح بتنفيذ الإصلاح القسري مباشرة على الإنتاج؛ يجب إنشاء فرع، تحديث الحزمة، ثم اختبار الكاميرا والبث والانضمام والخروج.
2. **حزمة selfbot غير مدعومة:** `discord.js-selfbot-v13` معلّمة deprecated وغير مدعومة. إضافة إلى المخاطر التقنية، تشغيل حسابات المستخدمين آليًا قد يخالف شروط Discord ويعرض الحسابات للإيقاف.
3. **قوة كلمات المرور:** الخادم يتحقق من وجود كلمة المرور فقط ولا يفرض حدًا أدنى للقوة. يجب فرض طول عشوائي مناسب عند النشر، ويفضل رفض كلمات المرور القصيرة عند الإقلاع أو توثيق سياسة إلزامية في Railway.
4. **الثقة في الـ proxy:** `trust proxy` مضبوط على قفزة واحدة. هذا صحيح فقط إذا كان التطبيق خلف Proxy موثوق واحد. تشغيله مكشوفًا مباشرة أو خلف عدد مختلف من الـ proxies قد يؤثر على `req.ip` و`req.secure` وحدود المحاولات وSecure Cookie.
5. **تخزين التوكنات:** التوكنات تُحفظ مشفرة، وهذا جيد، لكن قوة التشفير الفعلية تعتمد على سر `DATA_ENCRYPTION_KEY` أو `APP_PASSWORD`. يجب استخدام قيمة عشوائية طويلة وعدم الاعتماد على القيمة الافتراضية المحلية.

## ملاحظات منطقية إضافية

- الاختبارات الحالية قوية نسبيًا في بوابة الصوت، التأكيد، التدوير، وPlaying، لكنها لا تغطي مسارات Express كاملة، المصادقة والكوكيز، استعادة البيانات، تعارضات إعادة التشغيل، أو إدارة عمليات `Streamer`.
- الصورة المرفقة تُظهر حسابات برسالة `Account is not currently in a voice channel`. هذه الرسالة قد تكون صحيحة إذا كان الحساب خرج فعليًا، لكنها كانت أيضًا نتيجة محتملة للمشكلة السابقة: تنظيف الوسيط كان يزيل عضوية الروم ثم تأتي الدورة التالية قبل استعادة الجلسة. الإصلاح أعلاه يعالج هذا المسار، لكن يجب اختبار اتصال حي للتأكد من أن مكتبة البث لا ترسل مغادرة من داخل `joinVoice` أو من `Streamer` نفسه.
- توجد استدعاءات مقصودة لـ `stopSyntheticStream(..., { leaveVoice: true })` في عمليات الخروج اليدوي، تغيير الروم، فصل الحساب، وتغيير التوكن. هذه لا ينبغي إزالتها؛ الفرق أنها عمليات صريحة يكون فيها الخروج من الروم متوقعًا.

## خطة اختبار تشغيلية مقترحة

1. وصل حسابًا واحدًا بروم صوتي، ثم فعّل Stream، وانتظر 10–15 دقيقة مع تسجيل `data/media-events.log`.
2. تحقق كل 3 ثوانٍ من أن الحساب ما زال داخل نفس الروم، وأن `selfStream` ما زال صحيحًا.
3. أوقف مصدر الوسائط أو أعد تشغيل عملية FFmpeg عمدًا، وتأكد من أن الحساب لا يخرج من الروم وأن `media.restart_scheduled` يتبعه `media.ready`.
4. كرر السيناريو بالكاميرا، ثم جرّب الانتقال من كاميرا إلى Stream والعكس.
5. اختبر حسابين بالتوازي للتأكد من أن فشل وسيط حساب لا ينظف `Streamer` أو جلسة الحساب الآخر.
6. بعد نجاح الاختبار، راجع تحديثات `@dank074/discord-video-stream` في فرع مستقل لمعالجة تحذير `npm audit` دون استخدام `--force` على الإنتاج.

## الحكم النهائي

**الحالة الحالية بعد الإصلاح:** جيدة من ناحية بنية الكود والاختبارات الأساسية، لكن ليست جاهزة لاعتبار مسار الوسائط مضمونًا بالكامل قبل اختبار Discord/WebRTC حي. الإصلاح يمنع السبب الواضح لخروج الحساب من الروم أثناء تنظيف أو إعادة تشغيل الوسيط، ويجعل كل تشغيل يستخدم `Streamer` مستقلًا. الخطر الأهم المتبقي هو اعتماديات البث غير المدعومة والثغرات العالية العابرة، ثم نقص اختبارات التكامل الحية.

## تشخيص تغيّر التوكن عند كثرة المهام

المراجعة أظهرت أن التطبيق كان يضع عبارة `Token changed or invalid` بعد فشل تسجيل الدخول مرتين، دون التمييز الصارم بين رفض اعتماد حقيقي وبين انقطاع شبكة أو Gateway أو Rate Limit. هذا يجعل الخطأ الظاهر في الواجهة مضللًا؛ فالفشل المؤقت كان يُسجل وكأنه تغيير توكن، ثم ينفذ `markTokenChanged` ويفصل المهام والحساب.

تم تعديل هذا السلوك بحيث لا يتم وسم التوكن بأنه غير صالح إلا عند أخطاء اعتماد واضحة مثل HTTP 401 أو أكواد Discord المعروفة (`40001` و`4004`) أو رسائل صريحة عن Invalid Token. أما الأخطاء المؤقتة فتعاد للمستخدم كـ **Temporary Discord connection failure** ولا تؤدي إلى حذف جلسات الحساب أو إيقاف مهامه.

أضيف كذلك فاصل مشترك افتراضي مقداره 120ms بين عمليات Playing REST الأساسية عبر جميع الحسابات، ويمكن ضبطه بواسطة `DISCORD_REQUEST_GAP_MS`. هذا لا يجعل أتمتة حسابات المستخدمين آمنة أو مسموحة، لكنه يقلل اندفاع عمليات فحص الرسائل والإرسال عندما تعمل مهام كثيرة معًا.

وفق توثيق Discord، حدود الطلبات تطبق على المستخدمين والبوتات على مستوى المسار والمستوى العام، ويجب الاعتماد على `Retry-After` وحقول الاستجابة بدل تثبيت حدود صلبة. كما أن Discord يذكر أن طلبات 401 و403 و429 تعد طلبات غير صالحة ضمن حد IP، وأن أتمتة حسابات المستخدمين ممنوعة وقد تؤدي إلى إنهاء الحساب. لذلك إذا كان Discord قد غيّر توكنات فعلًا، فالاحتمال الأكبر هو اكتشاف أتمتة حسابات المستخدمين أو ضغط/أخطاء متكررة، وليس تصادمًا داخليًا بسيطًا في Map المهام.
